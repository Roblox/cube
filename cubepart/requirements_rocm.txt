# ROCm helper requirements.
#
# Use this inside a ROCm PyTorch environment, for example:
# rocm/pytorch:rocm6.4.3_ubuntu24.04_py3.12_pytorch_release_2.6.0
#
# This file intentionally does not install torch or torchvision; keep the
# ROCm-enabled builds that are already provided by the container.

scikit-build-core
cmake
ninja
pybind11
pybind11-global

# Keep NumPy and scikit-image ABI-compatible in Python 3.12 ROCm containers.
numpy==1.26.4
scikit-image==0.25.2
